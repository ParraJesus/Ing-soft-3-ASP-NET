@model List<CursoModel>

@{
    ViewData["Title"] = "Listar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Lista de Cursos
    </div>
    <div class="card-body">
        <a asp-action="Guardar" asp-controller="Curso" class="btn btn-success"><i class="bi bi-plus-circle"></i> Crear Nuevo</a>
        <hr />
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Profesor_id</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Nombre</td>
                        <td>@item.Profesor_id</td>
                        <td>
                            <a asp-action="Editar" asp-controller="Curso" class="btn btn-primary btn-sm" asp-route-Curso_id="@item.Curso_id">Editar</a>
                            <a asp-action="Eliminar" asp-controller="Curso" class="btn btn-danger btn-sm" asp-route-Curso_id="@item.Curso_id">Eliminar</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
